# Python
__pycache__/
*.py[cod]
.pytest_cache/

# uv / virtualenv
.venv/

# Synced docs + derived artifacts (CC BY-NC-ND — never commit; see spec-first-tdd/00-use-case.md)
data/

# macOS / editor junk
.DS_Store

# Local Claude Code settings
.claude/

# Demo recordings — review for on-screen PII before committing any of these
demo/

# Environment configuration
.env

# Temporary files and caches
temp/

# Local-only scratch content, anywhere in the tree (unanchored — matches at any depth)
_LOCAL_/
